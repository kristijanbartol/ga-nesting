\section{Method}
\label{sec:method}

\begin{figure*}[t]
\centering
\includegraphics[width=\linewidth]{pipeline-wip}
\caption{
  Overview of the proposed pipeline.
  The genome encodes seam keypoint positions $\boldsymbol{\delta}$, texture
  phase offsets $\boldsymbol{\kappa}$, and grain orientations
  $\boldsymbol{\rho}$.
  \emph{Geometry}: $\boldsymbol{\delta}$ is mapped via bilinear interpolation
  to mesh vertices; geodesics define the seam network and LSCM
  parameterization flattens the resulting patches.
  \emph{Stage~2}: Levenberg--Marquardt minimises seam phase residuals via
  small translational corrections per patch.
  \emph{Nesting}: patches are placed on the fabric roll by a bottom-left
  heuristic with positions snapped to the texture lattice.
  The GA scores each individual on fabric efficiency $f_\text{fabric}$,
  seam phase mismatch $f_\text{align}$, and fit preservation $f_\text{fit}$,
  and evolves the population with variable-specific operators.
}
\label{fig:pipeline}
\end{figure*}

% ---------------------------------------------------------------------------
\subsection{Problem Formulation}
\label{sec:formulation}

We define the problem as
\[
\Psi = \langle M, \mathcal{T}, \mathcal{L}, \mathbf{w}, \Omega \rangle,
\]
where $M = (V, E, F)$ is the 3D garment mesh
(we use SMPL~\cite{LoperMRPB15}),
$\mathcal{T}$ is a periodic fabric texture characterised by a lattice basis
$B = \{\mathbf{u}, \mathbf{v}\}$,
$\mathcal{L} = \{L_1, \dots, L_m\}$ is a set of admissible quad regions on
the mesh surface that constrain where seam keypoints may be placed,
$\mathbf{w} = \{w_s\}_{s \in \mathcal{S}}$ are static per-seam importance
weights ($w_s \in [0,1]$, set to zero for boundary seams),
and $\Omega = [0, W] \times [0, \infty)$ is the fabric roll domain of
width $W$.

% ---------------------------------------------------------------------------
\subsection{Texture Symmetry: Wallpaper Groups}
\label{sec:wallpaper}

The periodic structure of a fabric texture is fully characterised by its
wallpaper group $\mathcal{G}$, which encodes all translational, rotational,
and reflective symmetries of the pattern.
Accounting for $\mathcal{G}$ is essential for two reasons.
First, it constrains which grain-rotation pairs $(\rho_i, \rho_j)$ of adjacent
patches can produce a valid seam alignment: for a stripe pattern, rotating one
patch by 90° makes alignment impossible regardless of placement.
Second, patterns with glide-reflection symmetry (e.g.\ herringbone, chevron)
allow patches to align via a reflected copy of the texture, which must be
accounted for in the mismatch computation.

We support eight groups:
PM (stripes), PMM (grid/plaid), P4 (pinwheel), P4M (pinwheel with mirrors),
and three with glide-reflection symmetry---PG (herringbone), PMG (chevron),
PGG (brick bond).
Groups with rotational symmetry (PMM, P4, P4M) allow all grain-rotation
pairs $(\rho_i, \rho_j)$; the remaining groups require
$\rho_i \equiv \rho_j \pmod{2}$.
If $\mathcal{G}$ declares $(\rho_i, \rho_j)$ incompatible, the pair receives
a hard penalty in $f_\text{align}$.
For groups with glide-reflection symmetry (PG, PMG, PGG), each group defines
glide maps $\mathcal{G}_s = \{g_1, \dots\}$ acting on fractional phase
coordinates, and the per-point mismatch takes the minimum over the identity
and all glide images:
\[
\Delta_{\mathcal{G}}(\boldsymbol{\phi}_a, \boldsymbol{\phi}_b)
= \min\!\Bigl(
    \Delta(\boldsymbol{\phi}_a, \boldsymbol{\phi}_b),\;
    \min_{g \in \mathcal{G}_s} \Delta(\boldsymbol{\phi}_a, g(\boldsymbol{\phi}_b))
  \Bigr).
\]
For groups without glide symmetry, $\mathcal{G}_s = \emptyset$ and
$\Delta_{\mathcal{G}} = \Delta$.

% ---------------------------------------------------------------------------
\subsection{Genotype: Decision Variables}
\label{sec:genotype}

A candidate solution is encoded as
\[
\mathbf{x} =
\langle
\boldsymbol{\delta},\,
\boldsymbol{\rho},\,
\boldsymbol{\kappa},\,
\pi,\,
h
\rangle.
\]

\paragraph{Seam topology ($\boldsymbol{\delta}$).}
Each landmark region $L_j$ is a topological quad on the mesh surface defined
by four corner vertices.
The position of seam keypoint $k_j$ within $L_j$ is controlled by a 2D
parameter:
\[
k_j = \Gamma_j(\boldsymbol{\delta}_j), \quad
\boldsymbol{\delta}_j = (u_j, v_j) \in [0,1]^2,
\]
where $\Gamma_j$ performs bilinear interpolation within $L_j$ followed by a
snap to the nearest mesh vertex.
The full vector $\boldsymbol{\delta} \in [0,1]^{2m}$ concatenates all
landmark coordinates.
Seams $s \in \mathcal{S}$ are computed as geodesics between corresponding
keypoints.
Restricting keypoints to semantically defined regions prevents degenerate cuts
while still allowing the EA to explore meaningful seam placements.

\paragraph{Fabric orientation ($\boldsymbol{\rho}$).}
Cutting along seams yields $n$ 2D charts $P(\boldsymbol{\delta}) = \{p_1, \dots, p_n\}$.
Each chart is assigned a discrete grain orientation
\[
\rho_i \in \{0,1,2,3\}, \quad \theta_i = \rho_i \frac{\pi}{2},
\]
which determines how the chart is rotated on the fabric roll.
Compatibility with $\mathcal{G}$ is enforced at evaluation time.

\paragraph{Texture phase offsets ($\boldsymbol{\kappa}$).}
Because the fabric texture repeats periodically, placing a chart at different
positions that are integer multiples of the period apart produces
indistinguishable results.
We exploit this freedom by assigning each chart a discrete phase bin
\[
\kappa_i \in \{0,1,\dots,K-1\},
\]
where $K$ is the number of bins per period.
The bin determines a fractional offset $\kappa_i / K$ applied uniformly along
both lattice axes, effectively choosing which repeat of the texture the chart
is placed on.
Optimising $\boldsymbol{\kappa}$ allows the EA to align stripe or grid bands
across seams without moving the patches on the fabric.

\paragraph{Nesting variables ($\pi, h$).}
The permutation $\pi \in S_n$ defines the order in which charts are placed
on the fabric, and $h \in \mathcal{H}$ ($|\mathcal{H}|=3$) selects the
bottom-left placement heuristic variant.
In our experiments, $\pi$ and $h$ are fixed (area-sorted order and a single
heuristic, respectively) to concentrate the search budget on the more
impactful variables $\boldsymbol{\delta}$ and $\boldsymbol{\kappa}$, though
they remain evolvable in principle.

% ---------------------------------------------------------------------------
\subsection{Phenotype: From Genotype to Layout}
\label{sec:phenotype}

\subsubsection{Seam Cutting and Parameterization.}
Given $\boldsymbol{\delta}$, the keypoints $\{k_j\}$ are computed via
bilinear interpolation and snapped to mesh vertices.
Geodesic paths~\cite{SharpC20} between corresponding keypoints define the
seam network, along which the mesh is cut to yield 3D patches.
Each patch is then flattened to 2D by LSCM
parameterization~\cite{LevyPRM02}, with a fixed seam-straightness
regularization.

\subsubsection{Stage 2: Global Phase Alignment.}
\label{sec:stage2}
After parameterization, patches connected by seams with $w_s > 0$ form
connected components.
Within each component a small translational correction $\mathbf{c}_i \in \mathbb{R}^2$
per patch (root fixed at $\mathbf{0}$) is found by minimising the total
weighted seam phase residual via Levenberg--Marquardt, using wrapped
phase differences on $[-\tfrac{1}{2},\tfrac{1}{2})$ as residuals.
The corrections are baked into the patch vertices before nesting.
Stage~2 is applied identically in all baselines and in the GA, so any
improvement of the GA over baselines is not attributable to this stage.

\subsubsection{Nesting.}
Each chart is rotated by $R(\theta_i)$ and placed on the fabric roll using
heuristic $h$ in order $\pi$, subject to pairwise non-overlap and
containment in $\Omega$.
Placements are snapped to the texture lattice so that the absolute position
on the roll does not introduce an uncontrolled phase offset.

% ---------------------------------------------------------------------------
\subsection{Objective Functions}
\label{sec:objectives}

All objectives are minimised.
The weighted fitness sum used for selection tie-breaking and elitism is
\[
F_\Sigma(\mathbf{x}) = w_\text{fabric}\, f_\text{fabric}
                     + w_\text{align}\, f_\text{align}
                     + w_\text{fit}\, f_\text{fit},
\]
where $w_\text{fabric}, w_\text{align}, w_\text{fit} \geq 0$ are
designer-specified weights.

\paragraph{Fabric efficiency ($f_\text{fabric}$).}
\[
f_\text{fabric}(\mathbf{x}) = \frac{1}{NW} \max_{p \in \text{Layout}} y_{\max}(p),
\]
where $N$ is the number of garment bodies in the layout.
Normalising by $NW$ makes the metric comparable across body counts and
fabric widths.

\paragraph{Texture alignment ($f_\text{align}$).}
The 2D lattice phase of a fabric point $\mathbf{x}$ is
\[
\mathrm{Phase}(\mathbf{x}) = \mathrm{frac}([B]^{-1}\mathbf{x}) \in [0,1)^2,
\]
where $[B] = [\mathbf{u} \mid \mathbf{v}]$ and $\mathrm{frac}(\cdot)$ is
applied component-wise.
For chart $p_i$, the kappa offset shifts the phase uniformly:
$\mathrm{Phase}_i(\mathbf{x}) =
\mathrm{frac}(\mathrm{Phase}(\mathbf{x}) + \frac{\kappa_i}{K}\mathbf{1})$.
For stripe groups, the pattern is translation-invariant along the stripe axis,
so that phase component contributes zero to any difference.
The per-point mismatch is the average wrapped distance over both axes,
$\Delta(\boldsymbol{\phi}_a, \boldsymbol{\phi}_b)
= \tfrac{1}{2}\sum_{d} \min(|\phi_a^d - \phi_b^d|,\; 1 - |\phi_a^d - \phi_b^d|)$.
The seam mismatch integrates this over each seam arc-length:
\[
\mathrm{Mismatch}(s)
= \frac{1}{L_s} \int_0^{L_s}
  \Delta_{\mathcal{G}}\!\bigl(
    \mathrm{Phase}_i(\mathbf{x}_i(l)),\,
    \mathrm{Phase}_j(\mathbf{x}_j(l))
  \bigr)\, dl,
\]
and $f_\text{align}$ accumulates this over all seams weighted by importance:
\[
f_\text{align}(\mathbf{x}) = \sum_{s \in \mathcal{S}} w_s \cdot \mathrm{Mismatch}(s).
\]

\paragraph{Fit preservation guard ($f_\text{fit}$).}
Moving a seam keypoint away from the quad midpoint changes the 3D surface
area of the resulting patches.
To prevent the EA from improving texture alignment by shrinking the garment,
we penalise any \emph{reduction} in total 3D patch area relative to the
baseline configuration $\boldsymbol{\delta}_0 = 0.5$:
\[
f_\text{fit}(\mathbf{x})
= \max\!\left(0,\;
  \frac{A(\boldsymbol{\delta}_0) - A(\boldsymbol{\delta})}{A(\boldsymbol{\delta}_0)}
  \right),
\]
where $A(\boldsymbol{\delta}) = \sum_i \mathrm{Area}_{\mathrm{3D}}(p_i(\boldsymbol{\delta}))$
is the total surface area of all patches.
$f_\text{fit} = 0$ whenever the garment is at least as large as the baseline,
and positive otherwise.

% ---------------------------------------------------------------------------
\subsection{Evolutionary Algorithm}
\label{sec:ea}

The genotype mixes continuous ($\boldsymbol{\delta}$), discrete
($\boldsymbol{\kappa}$, $\boldsymbol{\rho}$), and combinatorial variables,
requiring variable-specific operators in the MIES
tradition~\cite{LiEEBSDR13}.

\paragraph{Selection.}
Tournament selection with size $k$ compares candidates first by Pareto
dominance: $\mathbf{x}$ dominates $\mathbf{y}$ iff
$\mathbf{F}(\mathbf{x}) \leq \mathbf{F}(\mathbf{y})$ component-wise with
at least one strict inequality.
When neither candidate dominates the other, the fallback criterion is the
weighted fitness sum $\sum_i w_i f_i$, providing a total order without
discarding multi-objective information.

\paragraph{Crossover (probability $p_c$).}
Each variable type uses a dedicated operator:
$\boldsymbol{\delta}$, $\boldsymbol{\rho}$, and $\boldsymbol{\kappa}$ use
uniform gene-wise swap (each gene exchanged with probability
$\tfrac{1}{2}$).

\paragraph{Mutation (probability $p_m$).}
Applied after crossover with operators matched to each variable type:
\begin{itemize}
    \item $\boldsymbol{\delta}$: Gaussian perturbation
          $\delta_j \leftarrow \mathrm{clip}(\delta_j + \mathcal{N}(0, \sigma_\delta),\,
          \delta_{\min}, \delta_{\max})$.
          The bounds $[\delta_{\min}, \delta_{\max}] \subset [0,1]$ keep
          keypoints away from degenerate mesh regions while the KDTree snap
          discretises each $\delta_j$ to a mesh vertex, creating a piecewise
          constant landscape with Voronoi-cell-sized neutral plateaus.
    \item $\boldsymbol{\kappa}$: each bin resampled uniformly from
          $\{0,\dots,K{-}1\}$ with per-gene probability $p_\kappa$.
    \item $\boldsymbol{\rho}$: each value resampled from $\{0,1,2,3\}$
          with per-gene probability $p_\rho$.
\end{itemize}

\paragraph{Elitism.}
The top $e$ individuals (by fitness sum) are carried unchanged to the
next generation, ensuring monotone improvement of the best-so-far
solution.

\paragraph{Search landscape characteristics.}
The landscape presented to the EA is unusual in two respects.
First, the $\boldsymbol{\delta}$ variables are nominally continuous but the
KDTree snap creates a discrete Voronoi tessellation: many neighbouring
$\boldsymbol{\delta}$ values map to the same mesh vertex, producing large
neutral plateaus.
Mutations that do not cross a Voronoi boundary have zero fitness effect,
making the convergence profile characteristically step-like rather than
smooth.
Second, the Stage~2 Levenberg--Marquardt solver absorbs much of the local
phase-correction signal, so that $\boldsymbol{\kappa}$ mutations contribute
mainly when they shift patches into qualitatively different phase
relationships that Stage~2 alone cannot reach.
The effective search is therefore dominated by $\boldsymbol{\delta}$
mutations that discover seam placements where the LSCM flattening produces
inherently better-aligned patch boundaries.
